import React from 'react';
// GlobalErrorHandler is simplified since we don't have backend API

/**
 * Global error handler - simplified version without backend integration
 * Since we're using JSON data, most error handling is not needed
 */
const GlobalErrorHandler = () => {
  // No-op component since we don't have backend API to monitor
  return null;
};

export default GlobalErrorHandler;

